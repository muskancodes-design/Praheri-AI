# Praheri.AI Backend — Python Dependencies
# Phase 1 prototype scope. Versions are pinned to minimum tested ranges;
# update deliberately and re-test the agent pipeline after any bump.

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.1

# Data validation
pydantic==2.6.1
pydantic-settings==2.1.0

# Database (planned — see Milestone M1; included now so the dependency
# set is ready when the data layer is implemented)
sqlalchemy==2.0.27
alembic==1.13.1
psycopg2-binary==2.9.9

# AI agent integration (planned — Milestones M2/M3)
anthropic==0.18.1

# Behavioral modeling
pandas==2.2.0
scikit-learn==1.4.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5
httpx==0.26.0

# Linting and formatting
ruff==0.2.1

# Environment management
python-dotenv==1.0.1
